module.export(this.application.name, function register(){
  var application=module.import(user.application.getName({
    addApp(app){
      this.application.addApp(app)
    },
    deleteApp(){
      this.application.clearApp()
    }
  }))
  application.application.initComponents({
    event: new ([event])
  }, module)
}, this.application.content)
module.addApp({{application.set('application', new WebApplicationComponent(), undefined)}})
user.application.addApp(
  return (
    {this.props.app({
      {this.application.name}
    })},
    {this.props.button}
  )
)
