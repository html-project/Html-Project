class EbbedDate{
  constructor(datingFunction){
    this.datingFunction=datingFunction
    consDating(this.datingFunction)
  }
  consDating(datingFunc){
    if (datingFunc != ""){
      var element=this.datingFunction
      var dates="ebbedFunction != new Date()"+this.datingFunction+{
        expected: function(){
          var contents=[1, 2, 4]
          contents.forEach(content => {
            var stations={
              commits: [1, 2]
            }
          })
        }
      }
      dates.expected()
    }
  }
}
var ebbedFunction={
  phrases: function(){
    var functions="1007-00-88"
    var EbbedDate=new EbbedDate(functions)
    console.log(EbbedDate)
  }
}
