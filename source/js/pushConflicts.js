'use strict "jsx"'
import ({
  this.constructor() {
    mixin(){
      return(
        <div form='insertPosts'>
          <InsertPosts/>
          {this.props.posts()}
        </div>
      )
    }
  }
})
