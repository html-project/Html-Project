module=new WebApplicationComponent({this.props.button}, {this.props.convert(event.allow({event.preventDefault('contentFunction', undefined)})), {this.props.footer})
module.insert({
  event: [event],
  user: new user()
})
renderEvent('post-body', {
  render(){
    return new user()
  }
}).set('application', new WebApplicationComponent(), function(){
  return (
    {this.property.id}
  )
})
