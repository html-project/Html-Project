if (module.delete('text-registered-users')){
  if (!module.export('binary-components', {{this.application.binary()}})){
    this.props.setBinary({this.application.master})
  }else{
    this.props.getComponent(module.import('application-component'))
  }
}else{
  module.export('registered-components', {this.registerers})
}
