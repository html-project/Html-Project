if (module.component){
  getBinary(function(){
    if (module.addComponent(event) ){
      module.create('text-registered-users')
    }else{
      module.create(this.props.userGroup)
    }
  })
  module.set('application-component', new WebApplicationComponent(), undefined)
}else{
  module.delete()
}
